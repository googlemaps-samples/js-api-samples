{
  "name": "@js-api-samples/event-domListener",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh event-domListener && bash ../app.sh event-domListener && bash ../docs.sh event-domListener && npm run build:vite --workspace=. && bash ../dist.sh event-domListener",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  }
}
