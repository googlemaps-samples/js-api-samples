{
  "name": "@js-api-samples/advanced-markers-html-simple",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh advanced-markers-html-simple && bash ../app.sh advanced-markers-html-simple && bash ../docs.sh advanced-markers-html-simple && npm run build:vite --workspace=. && bash ../dist.sh advanced-markers-html-simple",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {
    
  }
}
