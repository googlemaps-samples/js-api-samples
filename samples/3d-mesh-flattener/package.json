{
  "name": "@js-api-samples/3d-mesh-flattener",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh 3d-mesh-flattener && bash ../app.sh 3d-mesh-flattener && bash ../docs.sh 3d-mesh-flattener &&  npm run build:vite --workspace=. && bash ../dist.sh 3d-mesh-flattener",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {}
}