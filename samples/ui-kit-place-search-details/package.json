{
    "name": "@js-api-samples/ui-kit-place-search-details",
    "version": "1.0.0",
    "scripts": {
      "build": "tsc && bash ../jsfiddle.sh ui-kit-place-search-details && bash ../app.sh ui-kit-place-search-details && bash ../docs.sh ui-kit-place-search-details && npm run build:vite --workspace=. && bash ../dist.sh ui-kit-place-search-details",
      "test": "tsc && npm run build:vite --workspace=.",
      "start": "tsc && vite build --base './' && vite",
      "build:vite": "vite build --base './'",
      "preview": "vite preview"
    },
    "dependencies": {
      
    }
  }
  
