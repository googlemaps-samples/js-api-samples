{
  "name": "@js-api-samples/3d-polygon-extruded-hole",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh 3d-polygon-extruded-hole && bash ../app.sh 3d-polygon-extruded-hole && bash ../docs.sh 3d-polygon-extruded-hole &&  npm run build:vite --workspace=. && bash ../dist.sh 3d-polygon-extruded-hole",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {
    
  }
}
