{
  "name": "@js-api-samples/deckgl-kml",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh deckgl-kml && bash ../app.sh deckgl-kml && bash ../docs.sh deckgl-kml && npm run build:vite --workspace=. && bash ../dist.sh deckgl-kml",
    "test": "tsc && npm run build:vite",
    "start": "vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {
    "@loaders.gl/kml": "^4.4.1",
    "@deck.gl/core": "^9.3.1",
    "@deck.gl/google-maps": "^9.3.0",
    "@deck.gl/layers": "^9.3.0"
  },
  "devDependencies": {
    "apache-arrow": "^21.1.0"
  }
}
