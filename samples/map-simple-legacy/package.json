{
  "name": "@js-api-samples/map-simple-legacy",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh map-simple-legacy && bash ../app.sh map-simple-legacy && bash ../docs.sh map-simple-legacy &&  npm run build:vite --workspace=. && bash ../dist.sh map-simple-legacy",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {
    
  }
}
