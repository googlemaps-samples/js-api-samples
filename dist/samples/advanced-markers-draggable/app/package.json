{
  "name": "@js-api-samples/advanced-markers-draggable",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh advanced-markers-draggable && bash ../app.sh advanced-markers-draggable && bash ../docs.sh advanced-markers-draggable && npm run build:vite --workspace=. && bash ../dist.sh advanced-markers-draggable",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {
    
  }
}
