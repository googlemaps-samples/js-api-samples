{
  "name": "@js-api-samples/advanced-markers-collision",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh advanced-markers-collision && bash ../app.sh advanced-markers-collision && bash ../docs.sh advanced-markers-collision && npm run build:vite --workspace=. && bash ../dist.sh advanced-markers-collision",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {
    
  }
}
