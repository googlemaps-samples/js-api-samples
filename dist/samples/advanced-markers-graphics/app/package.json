{
  "name": "@js-api-samples/advanced-markers-graphics",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh advanced-markers-graphics && bash ../app.sh advanced-markers-graphics && bash ../docs.sh advanced-markers-graphics && npm run build:vite --workspace=. && bash ../dist.sh advanced-markers-graphics",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {
    
  }
}
