{
    "name": "@js-api-samples/ui-kit-place-search",
    "version": "1.0.0",
    "scripts": {
      "build": "tsc && bash ../jsfiddle.sh ui-kit-place-search && bash ../app.sh ui-kit-place-search && bash ../docs.sh ui-kit-place-search && npm run build:vite --workspace=. && bash ../dist.sh ui-kit-place-search",
      "test": "tsc && npm run build:vite --workspace=.",
      "start": "tsc && vite build --base './' && vite",
      "build:vite": "vite build --base './'",
      "preview": "vite preview"
    },
    "dependencies": {
      
    }
  }
  