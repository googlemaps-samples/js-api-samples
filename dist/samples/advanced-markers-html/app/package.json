{
  "name": "@js-api-samples/advanced-markers-html",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && bash ../jsfiddle.sh advanced-markers-html && bash ../app.sh advanced-markers-html && bash ../docs.sh advanced-markers-html && npm run build:vite --workspace=. && bash ../dist.sh advanced-markers-html",
    "test": "tsc && npm run build:vite --workspace=.",
    "start": "tsc && vite build --base './' && vite",
    "build:vite": "vite build --base './'",
    "preview": "vite preview"
  },
  "dependencies": {
    
  }
}
